# ---------------------------------------------------------------------------
# M-Control — Env template (copy to .env and fill in; add same vars to Vercel)
# ---------------------------------------------------------------------------

# Database — your Postgres URL (Vercel Postgres, Neon, Supabase, etc.)
DATABASE_URL="postgresql://user:password@host:5432/dbname?schema=public"

# Auth — at least 32 characters (use same on Vercel)
JWT_SECRET="your-super-secret-jwt-key-min-32-characters-long"

# Base URL — Vercel: https://your-project.vercel.app | Custom: https://m-control.biz
NEXTAUTH_URL="https://m-control.biz"

# Encryption for tokens — at least 32 characters (use same on Vercel)
ENCRYPTION_KEY="your-32-char-encryption-key-here!!"

# Optional — AI article generation (Phase 9). If unset, mock placeholder is used.
# OPENAI_API_KEY="sk-..."

# Optional
NODE_ENV=development
