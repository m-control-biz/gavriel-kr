# ---------------------------------------------------------------------------
# M-Control — Env template (copy to .env and fill in; add same vars to Vercel)
# ---------------------------------------------------------------------------

# Database — your Postgres URL (Vercel Postgres, Neon, Supabase, etc.)
DATABASE_URL="postgresql://user:password@host:5432/dbname?schema=public"

# Auth — at least 32 characters (use same on Vercel)
JWT_SECRET="your-super-secret-jwt-key-min-32-characters-long"

# Base URL — Vercel: https://your-project.vercel.app | Custom: https://m-control.biz
NEXTAUTH_URL="https://m-control.biz"

# Encryption for tokens — at least 32 characters (use same on Vercel)
ENCRYPTION_KEY="your-32-char-encryption-key-here!!"

# ---------------------------------------------------------------------------
# Google OAuth — covers Search Console, Google Analytics (GA4), and Google Ads
# Setup: https://console.cloud.google.com → APIs & Services → Credentials → OAuth 2.0 Client ID
# Authorized redirect URI: https://m-control.biz/api/auth/callback/google
# Enable APIs: Search Console API, Google Analytics Data API, Google Ads API
# ---------------------------------------------------------------------------
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# ---------------------------------------------------------------------------
# Meta (Facebook + Instagram) — pages, ads, instagram insights
# Setup: https://developers.facebook.com → My Apps → Create App → Business
# Valid OAuth Redirect URI: https://m-control.biz/api/auth/callback/meta
# Permissions: pages_read_engagement, instagram_basic, ads_read, business_management
# ---------------------------------------------------------------------------
META_APP_ID="your-meta-app-id"
META_APP_SECRET="your-meta-app-secret"

# ---------------------------------------------------------------------------
# LinkedIn — company page + ads
# Setup: https://www.linkedin.com/developers → My Apps → Create App
# Authorized Redirect URLs: https://m-control.biz/api/auth/callback/linkedin
# Products: Marketing Developer Platform, Share on LinkedIn
# ---------------------------------------------------------------------------
LINKEDIN_CLIENT_ID="your-linkedin-client-id"
LINKEDIN_CLIENT_SECRET="your-linkedin-client-secret"

# Optional — AI article generation. If unset, mock placeholder is used.
# OPENAI_API_KEY="sk-..."

# Optional
NODE_ENV=development
